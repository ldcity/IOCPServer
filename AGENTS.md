# Repository Instructions: IOCPServer

이 저장소는 `D:\GameProjects\portfolio-legacy` 공용 하네스의 일부다. 비단순 작업 전에 다음을 완전히 읽는다.

- `../.ai/CONSTITUTION.md`
- `../.ai/LEGACY_BASELINE.md`
- `../.ai/README.md`
- `../.agents/skills/cross-agent-development/SKILL.md`
- `../.ai/tasks/INDEX.md`가 가리키는 활성 task

이 저장소의 역할은 IOCP 네트워크 계층의 원형(LAN/WAN 분리, 패킷 암호화, lock-free 컨테이너)과 그 위에 단계적으로 확장한 채팅 서버다. 각 프로젝트가 라이브러리 사본을 따로 보유하므로, 활성 task가 소유하지 않은 프로젝트의 사본을 함께 고치지 않는다. 공용 파일이 없으면 하네스 누락을 보고하고 규칙을 추측하지 않는다.

`_Mail/`과 `PerformanceMonitor/`의 PDF·실행 파일은 외부에서 받은 자료다. 내용을 수정하지 않고, 공개 배포 가부는 사용자 판단에 맡긴다.

Codex의 기본 역할은 Claude 구현물의 독립 리뷰다. 리뷰 중 소스를 수정하지 않고 발견을 `../.ai/reviews/`에 먼저 기록한다.
